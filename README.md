# 🔐 Password Manager using Python & SQLite3

This project is a **simple password manager** built using **Python** and **SQLite3 (DB-API)**.  
It stores website credentials securely in a local database file and allows fetching saved passwords.

---

## 🚀 Features
- Create local SQLite database automatically
- Store website, username, and password
- Fetch all saved passwords
- Uses Python DB-API (`sqlite3`)
- No external database required

---

## 🛠️ Technologies Used
- Python
- SQLite3 (DB-API)

---

## 📦 Requirements
No external libraries required.  
SQLite3 comes **built-in with Python**.

---

## ▶️ How to Run

```bash
python password_manager.py
